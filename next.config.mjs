/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'lh3.googleusercontent.com', 'scontent-gru2-1.xx.fbcdn.net', 'platform-lookaside.fbsbx.com'],
    },
};

export default nextConfig;
